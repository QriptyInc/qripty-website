# dependencies
node_modules/

# build output and Astro's generated cache/types
dist/
.astro/

# local env files (secrets / per-machine overrides). .env and .env.development
# are committed: they hold only non-secret public config.
.env.local
.env.*.local

# Vercel CLI link and local settings
.vercel/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS and editor files
.DS_Store
Thumbs.db
.idea/
*.swp
