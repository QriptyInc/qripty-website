import { defineConfig } from 'astro/config';

export default defineConfig({
  // Static output — deploys to Vercel's free tier with zero config
  // (Vercel auto-detects Astro from this file + package.json).
  site: 'https://qripty.com',
});
