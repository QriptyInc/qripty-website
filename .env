# Non-secret public config, committed. Only PUBLIC_* names reach the browser.
# `npm run dev` overrides this via .env.development; put secrets or personal
# overrides in .env.local (git-ignored), never here.
PUBLIC_SCANNER_URL=https://scan.qripty.com
